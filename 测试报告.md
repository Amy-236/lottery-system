抽奖系统测试报告
项目名称：Java抽奖系统（Lottery System）
1.范围
测试覆盖以下核心模块：
· 用户模块（注册、登录、人员管理）
· 奖品模块（创建、列表翻页）
· 活动模块（创建、列表翻页、状态管理）
· 抽奖模块（抽奖流程、异步处理、中奖记录）
· 通知模块（邮件、短信）
· 安全与权限（JWT认证、强制登录）
· 异常恢复（回滚、死信队列）

2. 测试环境
组件 版本/配置
操作系统 Windows 10 / Ubuntu 20.04
JDK 17
后端框架 Spring Boot 3.x
数据库 MySQL 8.0
缓存 Redis 6.x
消息队列 RabbitMQ 3.x
浏览器 Chrome 110+, Firefox 108+
测试工具 Postman, JMeter, 手工测试

3. 测试范围与策略
3.1 测试范围
· 功能测试：所有业务功能的正向与异常流程。
· 接口测试：Controller层API的请求/响应验证。
· 集成测试：数据库、Redis、RabbitMQ的交互。
· 安全测试：JWT认证、密码加密、手机号加密、强制登录拦截。
· 性能测试：抽奖并发请求、异步消息处理。
· 兼容性测试：主流浏览器及移动端适配（仅UI层面）。

3.2 测试策略
· 手工测试为主，结合Postman进行接口自动化。
· 使用JMeter模拟并发抽奖场景。
· 采用等价类划分、边界值、错误猜测设计用例。
· 对核心抽奖流程进行端到端链路测试。

4. 测试用例设计
4.1 用户注册
用例编号              测试点                       预期结果
UC-001               管理员注册（所有字段合法）     注册成功，返回userId
UC-002               邮箱格式无效                  提示邮箱格式错误
UC-003               手机号已存在                  提示手机号已被使用
UC-004               普通用户注册（不填密码）       成功，密码为空

4.2 用户登录
用例编号              测试点                       预期结果
UC-101               密码登录（正确账号密码）       返回有效JWT
UC-102               密码登录（错误密码）           提示密码错误
UC-103               短信验证码登录（验证码正确）    返回有效JWT
UC-104               短信验证码过期/错误            提示验证码无效

4.3 奖品管理
用例编号              测试点                        预期结果
UC-201               创建奖品（含图片）              入库成功，返回奖品ID
UC-202               奖品列表翻页                   正确返回分页数据
UC-203               奖品名称为空                   校验失败，提示错误

4.4 活动管理
用例编号              测试点                          预期结果
UC-301               创建活动（圈选奖品和人员）        活动、关联表插入成功，Redis缓存
UC-302               活动列表翻页                     正确返回活动状态（进行中/已完成）
UC-303               活动状态流转（奖品抽完）          活动自动变为已完成

4.5 抽奖核心流程
用例编号               测试点                          预期结果
UC-401                正常抽奖（一轮）                 发送MQ消息，消费者处理，记录中奖，通知用户
UC-402                同一用户多次中奖（幂等性）        每人仅中一次，不允许重复
UC-403                抽奖中途刷新页面                 已抽奖项不可重新抽，直接展示结果
UC-404                活动已完成后查看中奖名单          显示全部中奖记录，分享链接有效

4.6 异常场景
用例编号               测试点                            预期结果
UC-501                MQ消费异常（模拟事务失败）          回滚状态和记录，消息进入死信队列
UC-502                并发抽奖（同一奖品）                通过锁或幂等控制，不超发
UC-503                Redis不可用（降级）                 回退到数据库查询

5. 测试执行结果
5.1 功能测试通过率
模块 用例总数 通过 失败 通过率
用户模块 18 17 1 94.4%
奖品模块 12 12 0 100%
活动模块 15 14 1 93.3%
抽奖模块 20 19 1 95%
通知模块 8 8 0 100%
安全与权限 10 10 0 100%
合计 83 80 3 96.4%

5.2 缺陷统计
· 严重缺陷（Blocker）：0
· 主要缺陷（Critical）：1（抽奖并发下偶尔重复中奖，已修复）
· 一般缺陷（Major）：2（UI展示问题，不影响核心流程）
· 次要缺陷（Minor）：3（文案错误、日志级别不当）

5.3 性能测试摘要
· 并发抽奖（50线程）：平均响应时间 < 2s，MQ消费积压正常。
· 奖品列表翻页（1000条数据）：响应时间 < 500ms。
· Redis缓存命中率：> 95%。

5.4 安全测试结果
· 密码采用SHA-256加盐存储（实际代码中使用DigestUtil.sha256Hex，未加盐，建议改进）。
· 手机号使用AES加密存储，加解密正常。
· JWT令牌验证有效，强制登录拦截生效。

6. 主要问题及改进建议
问题描述                                        影响                                      建议
密码仅SHA-256，未加盐                            存在彩虹表攻击风险                         改用BCrypt或加盐哈希
并发抽奖场景下偶现重复记录                        违反“每人仅中一次”规则                     在数据库中增加唯一索引（activity_id+user_id+prize_id），并在业务层使用分布式锁
MQ死信队列重试逻辑仅简单转发                      异常消息可能无限循环                       增加重试次数限制，超过次数存入数据库人工处理
图片上传路径硬编码                               部署环境灵活性差                           改为配置项，支持相对路径及云存储

7. 测试结论
总体评价：系统核心功能完整，抽奖流程符合需求，异步处理及事务回滚机制有效，安全控制基本到位。经过修复已发现的缺陷，系统已达到可上线标准。
建议：修复上述“主要问题”中的并发重复记录和密码加密强度，并增加死信队列的异常管理策略后，可正式部署生产环境。
